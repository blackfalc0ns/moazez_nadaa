# Dismissal Notifications Backend Gaps

No critical backend gaps were found for the current dismissal notifications flow.

The app uses `sourceType = dismissal_request` and `sourceId` or a direct `requestId` to open the related dismissal request details.
